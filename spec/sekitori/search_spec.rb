require "spec_helper"

RSpec.describe Sekitori::Search do
  it "has a version number" do
    expect(Sekitori::Search::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
